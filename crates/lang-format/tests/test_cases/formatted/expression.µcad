1 + 2;
(1 + 2);
((1 + 2) + 3);
-1;
-(1 + 3);

std::geo2d::Circle(40mm);

attribute#access;
array[access];
tuple.access;
method.call(10mm);

builder
    .foo()
    .bar()
    .baz()
    .bof();

a_more_complex_builder
    .foo(
        a = 1mm,
        b = 2mm,
        c = 3mm,
        d = 4mm,
        e = 5mm,
    )
    .bar(a = 1, b = 2)
    .baz()
    .bof();
