use std::geo2d::*;
use std::ops::*;

Circle(7mm).extrude(20mm) &
Circle(1.5mm).translate(x = 5mm).__builtin::ops::spiralize(
    height = 20.0,
    turns = 5.0, 
    inner_radius = 1.0,
    outer_radius = 1.0,
);
