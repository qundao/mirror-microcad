use std::geo2d::*;
use std::ops::*;

Circle(7mm).extrude(20mm) &
__builtin::geo2d::ThreadProfile(3.0).rotate(-90°).translate(x = 5mm).__builtin::ops::spiralize(
    height = 45.0,
    turns = 15.0, 
    inner_radius = 1.0,
    outer_radius = 1.0,
);
