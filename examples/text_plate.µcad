use std::geo2d::*;
use std::ops::*;
use std::math::*;

sketch TextPlate(text: String, height: Length = 30mm) {
    letters = Text(text, height).align();
    plate = RoundedRect(10mm, radius = 20mm);
    plate - letters
}

TextPlate("Hello µcad", 3mm)
    .translate(x = 10mm)
    .revolve(30°);
